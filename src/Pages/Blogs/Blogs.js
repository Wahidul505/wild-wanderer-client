import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h1>Difference Between Authentication and Authorization?</h1>
            <p>• Authentication is about validating someones credential to verify identity. Authentication </p>
        </div>
    );
};

export default Blogs;